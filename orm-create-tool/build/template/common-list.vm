package ${outputor.param.packageNameList};

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import java.io.Serializable;

/**
*$!{outputor.comment}实体列表
*/
public class ${outputor.className}List extends ArrayList<${outputor.className}> implements Serializable{
    private static final long serialVersionUID = ${outputor.serialVersionUID}5L;

    public ${outputor.className}List() {
        super();
    }

    public ${outputor.className}List(int size) {
        super(size);
    }

    public ${outputor.className}List(List<${outputor.className}> list) {
        Collections.addAll(this, list.toArray(new ${outputor.className}[]{}));
    }
}